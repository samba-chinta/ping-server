<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../public/main.css">
    <title>MFA Setup</title>
</head>
<body>
    <div class="container">
        <div class="main-wrapper">
            <div class="qr-wrapper">
                <p>Scan this QR to add the app</p>
                <!--Setting up the QR Code URL generated 'speakeasy' library-->
                <img src="<%= qrURL %>" alt="Profile Picture">
            </div>
            <div class="verify-form__wrapper">
                <!--
                    Form to take the input i.e TOTP from the user
                    for the initial validation, which is useful in
                    enabling the MFA for respective user
                -->
                <form method="post" action="/verify-mfa">
                    <input type="text" placeholder="Enter the TOTP generated by Auth App"/>
                    <button type="submit">Verify and Enable MFA</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>