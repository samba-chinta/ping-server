<!DOCTYPE html>
<html lang="en">
    <body>
        <div class="container">
            <div class="main-wrapper">
                <div class="qr-wrapper">
                    <div class="qr-main">
                        <!--Setting up the QR Code URL generated 'speakeasy' library-->
                        <img src="<%= qrURL %>" alt="QR Image" />
                    </div>
                </div>
                <div class="verify-form__wrapper">
                    <!--
                    Form to take the input i.e TOTP from the user
                    for the initial validation, which is useful in
                    enabling the MFA for respective user
                -->
                    <form method="post" action="http://localhost:5000/auth/verify-mfa">
                        <p class="important-notes">
                                Scan the QR code using a Auth app (Example:
                                Google Authenticator, Zoho OneAuth, Microsoft
                                Authenticator) to add secret code in Auth App.
                                Then enter the "Time-based One Time
                                Password"(TOTP) generated by Auth App to verfiy
                                and enable the MFA in your PING App.
                        </p>
                        <input
                            type="text"
                            placeholder="Enter the TOTP"
                            name="totp"
                        />
                        <!--Will removed this field (only for testing)-->
                        <input
                            type="text"
                            name="token"
                            value="<%= token %>"
                            class="token"
                        />
                        <button type="submit">Verify and Enable MFA</button>
                    </form>
                </div>
            </div>
        </div>
    </body>
</html>
